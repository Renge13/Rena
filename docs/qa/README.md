<!--
STATUS: THE INDEX. Hand-written, because "what question does this answer" is a judgement no
generator can make. Its COVERAGE is machine-checked: `npm run check:qa` fails if an artifact in
this directory has no row here, or if a row names a file that no longer exists in either
docs/qa/ or docs/qa/superseded/.
Adding an artifact to docs/qa/ without adding its row is a failing build, not a nit.
-->

# docs/qa — the evidence index

**Nothing in this directory is ever edited and nothing is ever deleted.** These files are
evidence: an edited render is not one, and a deleted trace cannot be re-cited. Superseded
artifacts move to `superseded/`, they do not disappear.

## THE NAMING RULE, ruled by Reyner 2026-08-22

> **No two artifacts may differ only by a plural, a suffix letter, or a version digit.**

It cost a real mistake. `2026-08-22-renders-n10-postfix.md` and
`2026-08-22-renders-n10-postfixes.md` differ by one `s`, describe two different traces with two
different floor rates, and **sent Reyner to the wrong file while he was forming a sell/no-sell
verdict.** A citation that resolves to the wrong evidence is worse than a missing one, because
the argument still completes.

**Enforced in `scripts/check-qa-artifacts.mjs`, which runs in `npm test`** - the same reasoning
as the harness refusing to overwrite evidence rather than documenting that it should not. A
naming convention in a doc is a convention nobody reads at the moment they pick a filename.

**Name an artifact for what it MEASURES, never for which attempt it was.** `-postfix` /
`-postfixes` / `-verify2` / `-v2` are all that mistake. The one existing collision is
grandfathered with its reason in that script, because renaming a cited file breaks the
citations the rule exists to protect.

---

## Current

| Artifact | What question it answers | prompt | gate | Superseded by |
|---|---|---|---|---|
| `2026-08-17-renders.md` | The first real end-to-end renders. Does the chain produce servable prose at all? | - | - | - |
| `2026-08-18-rejections.md` + `2026-08-18-rejections.json` | WHICH Stage 6 checks actually fire, and how often, over a large trace. The rejection-cause census. | - | `1.9.0` | - |
| `2026-08-18-retry-depth.md` + `2026-08-18-retry-depth.json` | Does a regeneration buy a lower floor rate, and how much per extra depth? The depth curve, and the trace whose stored prose later served the depth-pair reads. | - | `1.9.0` | - |
| `2026-08-19-renders.md` | The renders Reyner read for THE READ. | - | `1.10.0-floor` | `2026-08-19-THE-READ.md` |
| `2026-08-19-THE-READ.md` | The five readings Reyner judged as the buyer, verbatim, with the banners. | - | - | - |
| `2026-08-19-READ-VERDICT.md` | Reyner's verdict and five rulings from THE READ. The document every later content tranche traces to. | - | - | - |
| `2026-08-19-retry-erosion.md` + `2026-08-19-retry-erosion.json` | What does a regeneration COST the prose? The findings-erosion measurement, at depth 2. | `2ff1a546fb7e6e53` | `1.9.0` | - |
| `2026-08-21-floor-cause-analysis.md` | Why each floored run floored, cause by cause, rather than as a rate. | - | `1.13.0` | - |
| `2026-08-21-renders.md` | Renders after the 08-21 gate work. | - | - | `2026-08-21-renders-n10.md` |
| `2026-08-21-renders-rule23-inserted.md` | Does the rule-23 bracket check reject real readings, and which ones? | - | - | - |
| `2026-08-21-renders-n10.md` | The first n=10 trace. Is a floor RATE measurable at all, given that n=1 returned 0/4, 2/4 and 1/4 on identical inputs? | `2ff1a546fb7e6e53` | `1.13.0` | `2026-08-22-renders-n10-postfixes.md` |
| `2026-08-22-renders-n10-postfix.md` | An n=10 trace taken mid-session, BEFORE the final post-fix gate. **Not the artifact precondition 3a was measured on** - see the row below, and read the naming rule above. | - | - | `2026-08-22-renders-n10-postfixes.md` |
| `2026-08-22-renders-n10-verify2.md` | A verification n=10 trace; the source of the "`fact.condition_named` survives the whole budget" reading that the erosion ladder later corrected. | - | - | `2026-08-22-renders-n10-postfixes.md` |
| `2026-08-22-renders-n10-budget3.md` | Does `REGENERATION_BUDGET` 3 move the floor rate? It did: pooled 20% -> 10%, against a docblock prediction of "little or not at all". | - | - | - |
| **`2026-08-22-renders-n10-postfixes.md` + `2026-08-22-renders-n10-postfixes.json`** | **THE artifact precondition 3a was measured on: pooled floor 4/40 = 10% at n=10.** The JSON stores all 72 attempts of the 40 runs, which is what makes every zero-cost artifact below possible. | `22316c3349d0ea46` | `1.17.0` | - |
| `2026-08-22-depth-1-vs-3-postfixes.md` | Does a reader who waited for two extra regenerations get a WORSE reading? The depth-pair read Reyner ruled on: *"depth 3 is thinner, not tighter."* Zero cost, from the postfixes JSON. | `22316c3349d0ea46` | `1.17.0` | - |
| `2026-08-22-owed-samples.md` | Two readings the harness stored but never printed: a fresh-1996 run WITHOUT the opening flag (the printed one had it, 2 of 10 do), and a chart-5 run that is a real render rather than the floor. Zero cost. | `22316c3349d0ea46` | `1.17.0` | - |
| `2026-08-21-renders-rule23-enforced.md` | What did rule-23 bracket-once ENFORCEMENT do to the renders, and what did it cost? **This is #53s reopen baseline**, and it existed only on the closed `feat/rule23-enforced` branch until `986c450` - the evidence was left behind the question it settles. **READ THE GATE COLUMN BEFORE COMPARING ANYTHING TO IT:** measured at `1.12.0`, and `main` is five versions on, so a fresh run against these numbers is not like for like. `main` carries the `-inserted` variant, which is a different measurement. | `2ff1a546fb7e6e53` | `1.12.0` | - |
| `2026-08-31-head-fit.md` | Does any archetype head overflow the 936px measure at full size? Prompt R section 0a replaced a word-count proxy with a real-fit gate; this is the measurement it rests on. Headless Chromium with real Archivo, refusing to print widths if the font did not load. **ALL TEN FIT, so the pinned overflow set is empty.** Records the finding that killed the proxy: MOUNTAIN at 793.48px OVERFLOWED the old 763 measure by 30.48px and was never reduced, while MORNING fit with 46px spare and was. | n/a | n/a | - |
| `2026-08-31-watermark-fit.md` | Where does the watermark glyph INK actually land on the 1080x1350 frame, and does it still cross the headline and reach the tag row? Prompt R section 0b. Each card rendered twice and differenced, because the div box is not the ink box. **Both requirements hold on all ten at the inherited offsets, which are therefore kept - a derivation whose answer is unchanged, not an assumption.** Records the band (top in -186..-128), the 癸 bound this same commit created, and why 丁 not running off-card is a description rather than a failure. | n/a | n/a | - || `2026-08-27-floor-after-heading-ruling.md` | What does a reader actually GET when the provider fails, now that the heading names the fact and the body opens on the meaning (RULED 2026-08-26)? A floored reading served through the real routes and the real page, verbatim. **Served for zero dollars with an INVALID Gemini key** - the method is recorded in the artifact, because the same arrangement is a live production failure mode: the render fence tests that a key is PRESENT, never that it WORKS. | n/a | `1.17.0-floor` | - |
| `2026-08-26-card-b-overflow.md` | Does the paid card's prose fit, on every chart rather than the one that reported it? **It did not: 9 of 13 fixture charts overflowed, worst +121px, and the reported chart ranked 7th of 13 by prose.** Records the reclaim ledger, the proof that Card A is byte-identical, and verification past the fixture with a maximum-prose card per stem. **Records one open number: 7px of slack on 癸 at maximum prose.** | n/a | n/a | - |
| `2026-08-26-card-capture-verification.md` | Does the capture fix work, can it be broken again on demand, and does the page still render at desktop and phone width? The un-fix reproduces `distinct: 1` on both cards; the probe goes 16 findings -> 0 -> 16 -> 0 across the four builds. Carries the two exported cards as committed images, because a PR body cannot render a relative image path. **Records one still-open defect: Card B's content is 85px taller than the card.** | n/a | n/a | - |
| `2026-08-31-upcoming-seen-observed.md` | Does `upcoming_seen` - the DENOMINATOR of both September interest rates - actually fire? Prompt Q commit 4 could only prove it did NOT fire on mount; the agent browser pane runs `visibilityState: "hidden"`, where an IntersectionObserver never delivers, so the firing half had never been observed at all. Driven through real headless Chromium over CDP with no new dependency, at 375x812. **Both halves in one run: absent at load with the block 4551px below the fold, present exactly once after scroll, and not again on re-entry.** Records that the harness itself shipped a check that passed on a mount-firing build until the neuter run exposed it. | n/a | n/a | - |
| `2026-08-26-card-capture-cause.md` | Why the free share button produced a blank rectangle. Not a render artifact and it carries no prompt or gate version: it measures the CARD CAPTURE, which no gate ever looked at. Establishes the cause (html-to-image relayouts the clone to the output size, and the canvas centres its child), refutes the stopgap in `prompts/O-amend-card.md` (the download target is damaged too, 19.8% of pixels), and proves the paid Card B was never on the broken path. | n/a | n/a | - |
| `2026-09-03-skeleton-to-prose-gap.md` | Is the region really blank when the prose skeleton hands over, and is `.k-rise` the cause? Required by Reyner's 2026-09-02 prompt BEFORE the fix was built, with an instruction to stop and report if the cause did not hold. **The cause HOLDS - the reduced-motion counterfactual puts opacity at 1 immediately - and the stated MAGNITUDE does not: the blank is one paint plus ~100ms under half opacity, not "close to a second", which was the 0.8s duration read as a gap.** Records two instrument failures caught in the act: frame sampling is impossible in the agent's browser pane (rAF never fires, `setTimeout(50)` took 902ms), so a 16ms sampler would have returned ~1 sample per second and fabricated a number without erroring; and the first pass measured `<p>` opacity when `.k-rise` is on the wrapper, reporting no defect at all. The working instrument is the Web Animations API, seeked rather than sampled. | n/a | n/a | - |
| `2026-09-03-submit-to-chart.md` | With `delay(2500)` deleted, what IS the submit-to-chart time? Nobody had ever measured it: the pause was never work, so it masked the real figure for the funnel's whole life. **Median 112ms, range 56-716ms** - the removed constant was ~96% of that wait. Breaks out `/api/season-check`, which the pause had been hiding inside a `Promise.all` and which is now the largest term. **Records that its ~500ms spikes are real network durations but NOT the handler's work** - twenty rapid calls give median 14.8ms, p90 18.4ms, 0/20 over 200ms, so the spikes are `next dev` idle behaviour and the route costs about 15ms. Carries the check that the hidden pane's 1Hz timer throttling did not distort the render timing, and the warning that the chart does not snap in at 112ms because `.k-fade` then eases it over 0.5s. | n/a | n/a | - |

| `2026-09-05-production-submit-split.md` | **PR2 STEP 1.** What is the 4.9s submit wait actually made of, split per request, on PRODUCTION? Taken on `main` = `2dae13b` (NOT the `f29fc0e` the prompt named; the range is two docs plus a comment-only block, so the submit path is byte-identical and the recording stays comparable). **`POST /api/mirror` is 88.4% of the warm wait and it is three sequential Supabase round trips run in `iad1` against a database in `ap-southeast-1`** - `x-vercel-id` reads `sin1::iad1` and `vercel.json` declared no `regions`. Cold n=1 5,022.6ms (reproducing the recorded 4.9s), warm n=6 median 2,807.1ms, never averaged together. **Falsifies the register row that quoted 112ms as `~100ms after the tap`, and prices the prompt's own proposed fix at ~10.5% of the wait.** Records that a region change also moves the function-to-Gemini distance, so it must be baselined on the prose leg too, with cache hits labelled. Carries three instrument controls (empty-buffer control, a 400-not-429 probe control, MutationObserver agreeing with resource timing to 3.8-9.3ms) and two corrections made by running the command rather than trusting it. | `T-arrival` | n/a | - |

| `2026-09-05-region-move-both-legs.md` | Did `regions: ["sin1"]` (`63d6488`) actually fix the submit wait, and did it cost anything on the prose leg? Before/after on BOTH legs, because the move changes the function-to-Gemini distance as well as the function-to-database one. **tap -> chart 2,776.5ms -> 240.5ms (11.5x, warm median n=6); prose cache HIT 3,263ms -> 275.5ms (11.8x); prose cache MISS 9,300/15,352 -> 6,097/8,811, no regression detected.** Every prose run labelled by `meta.cached` off the response and confirmed `source: "gemini"` rather than a floor, because a before/after built from cache hits would show the Supabase win and hide a provider regression. Verifies `x-vercel-id` moved `sin1::iad1` -> `sin1::sin1` BEFORE measuring. **States plainly what it does not establish:** a miss total contains both halves, so the provider call could have slowed and still shown a net win - isolating it needs in-handler timing. Withholds the cold label rather than claiming a 16x cold win, because the deployment had serve traffic. Checks the baseline's projections against measurement (~20ms projected vs ~46ms actual per intra-region round trip; ~50-80ms projected vs 51.2ms actual for the season-check fold) and records that Reyner's ~1s waiting-state threshold is no longer met. | `T-arrival` | n/a | - |

| `2026-09-05-workers-compatibility-spike.md` | Can Katon run on Cloudflare Workers, and if not, what? One bounded session under a decision rule fixed BEFORE the evidence, so the result could not be rationalised. **Answer: RENDER, and the service must be in Singapore** - `63d6488`'s 11.5x came entirely from collocating with `ap-southeast-1`, and a deployment in the wrong region hands it back silently. Carries `npx vinext check` verbatim (83%, 1 issue) and shows **the check is wrong in both directions**: its only ✗ is a false positive against three stale `.claude/worktrees/` branches, including a file deleted in the 2026-08-23 promotion, while its clean verdict never examined runtime `fs`, `node:crypto` or `@react-pdf/renderer` at all. Two runtime disk reads found in shipped code (`lib/pdf/fonts.js:85`, `lib/render/prompt.js:86`). **The load-bearing finding is `PROMPT_VERSION`:** it is a module-scope SYNCHRONOUS const, and because `prompt_version: null` is already legitimate for floor renders, an async Web Crypto port would let a real Gemini render stamp `null` indistinguishably from a floor - nothing goes red, no test fails, and "which prompt produced this reading" silently stops being answerable. Also records that card capture is client-side (`html-to-image`) and therefore host-independent, and that the rule caught its own author mid-estimate. | `T-arrival` | n/a | - |
| `2026-09-07-compat-base-rates.md` | How often does each ruled compat clause ACTUALLY fire? Every clause in tranche 1 and 2a was tested on ONE hand-derived pair, which proves it is implemented and says nothing about whether it fires for 2% of pairs or 95%. 2000 seeded random birth datetimes (1960-2005, hour known), 5000 random ordered pairs, on `main` = `a25ccf5`. **MEASUREMENT ONLY - no rule changed, no code touched in response.** Headline: **`2.2.a` never fails, 0 of 5000** - it is a conjunct of FIT that is always true, so removing it would not move a single quadrant, which is a finding about the RULE and is stronger than the fixture gap tranche 1 and 2a both recorded. **Pull is high for 3 in 4 pairs and 2.1.d supplies almost all of it** (72.1% against 74.9% overall), because 2.1.d scans eight branch pairings and asks only for harmony or clash. **q1 is 66.3% and 88% of pairs land in q1 or q3**, so q2 (the addictive-hard one, the best content) is 8.6% and q4 is 3.4% - worth knowing BEFORE the four quadrants' unwritten content is authored. `contrasting` at 80.6% is arithmetic over five Ten God groups, not a defect. **Shown failing TWICE on purpose before its numbers were believed**: the known-answer selftest driven red by corrupting one expected quadrant, and the invariant check driven red by dropping ~1 in 97 tally increments (`INVARIANT BREACH: quadrants sum to 494, not 500`). Two consecutive runs byte-identical; a different seed moves the numbers. **States its own limit**: these are rates over RANDOM pairs, not couples, and Katon has no compat traffic to measure. **SECOND RUN APPENDED same day, same seed and same 5000 pairs, six rule sets in one table** (V0 current, V1 drop 2.1.d, V2 2.1.d = month branch only, V3 V1 + palace 害/刑 lower fit, V4/V5 V3 with 2.2.a tightened two ways). **It REFUTES the first run's own suggested fix:** tightening 2.2.a does not rescue it - rank-0 holds 99.5% and both-directions 99.9%, so V3/V4/V5 differ by at most 4 pairs in 5000. Also measured: **2.1.a and 2.1.b NEVER fire alone, 0 of 5000** (found when a deliberate corruption of local V0 failed to trip the guard, then measured on purpose rather than left inferred; 2.1.d 3607 + 2.1.c-alone 136 = 3743 = every pull-high pair). Palace 害/刑 present on 57.3%, so V3 swings fit-high 88.1% -> 42.4% and puts 43.9% of pairs in q4 - the mirror of V0's problem, not a fix. **Two new guards driven red on purpose** (local V0 vs the shipped module, per-pair monotonicity across variants), and the local-V0 guard's SENSITIVITY LIMIT is recorded: it caught a dropped 2.1.d but not a dropped 2.1.b. The first run's 165 lines are unedited - appended to, per this index's no-edit rule. **RUN 2b APPENDED 2026-09-08** on Reyner's instruction, adding **V6 = V2's pull (2.1.d month-branch only) + V3's fit (palace 害/刑 lower fit) with 2.2.a DELETED** - the combination run 2 named and did not measure. Same seed, same 5000 pairs; runs 1 and 2 byte-identical afterwards. **V6 is the only measured shape where all four quadrants carry real weight: q1 21.5% / q2 25.7% / q3 20.9% / q4 31.9%**, spread 20.9-31.9 against V0's 66.3/8.6/21.8/3.4. q2 - the addictive-hard quadrant with the best content - goes 8.6% -> 25.7%. **Deleting 2.2.a moved fit-high by ZERO** (V6 42.4% = V3 42.4%), confirming run 2's inertness finding in its third form. Two new guards driven red on purpose: `V6.pull === V2.pull` identical-not-ordered (trips on pair 2 when V6 keeps the unrestricted 2.1.d) and `V3.fit => V6.fit`, the OPPOSITE direction from V4/V5 because dropping a conjunct can only ADD fit (trips on pair **1208** when 2.2.a is re-added - recorded because it needed most of the sample to find one of ~4 differing pairs, which is this guard's sensitivity limit stated). **No rule moved and nothing in `lib/` changed** in runs 1, 2 or 2b. **RUN 3 APPENDED 2026-09-08 AND IT IS THE FIRST ONE THAT VERIFIES RATHER THAN EXPLORES:** Reyner RULED V6 that day, `lib/compat/pullFit.js` now implements it, and run 3 asks only whether the shipped module reproduces run 2b's V6 row. **It does, to the pair** - 1074 / 1284 / 1047 / 1595, pull 47.2%, fit 42.4%, checked mechanically against the committed run-2b table rather than by eye. `V0` (asserted per pair against the SHIPPED module) and `V6` (the local hypothetical) are now the same rule set and print identical rows, which is the verification; `Vpre` is added so run 1's pre-amendment table stays comparable in the same view. **The local-V0 guard FIRED FIRST and that firing was the proof the module had changed**, and a second invariant caught a stale `FIT_CLAUSES` constant still listing the deleted 2.2.a. **The finding worth carrying forward: 2.1.a and 2.1.b now fire ALONE on 4.5% and 6.0% of pairs, having fired alone on 0 under the old rule** - run 2's finding 2 said they were redundant only in the presence of the wide 2.1.d and warned against deleting them on that basis; narrowing 2.1.d made both load-bearing on 525 pairs. | `X-compat-2a` | n/a | - |
| `2026-09-08-compat-renders-n10.md` | The FIRST floor rate for the pair pipeline. Ten fixture pairs, one draw each, all four quadrants and four `p2_reframe_required` pairs, on `STAGE6_VERSION 1.18.0` / pair prompt `9109d65d14b74e46` / `gemini-3.1-flash-lite`. **POOLED FLOOR RATE 2/10 = 20%, ABOVE the mirror's ruled 10% bar, and NOTHING WAS LOOSENED to move it** - prompt X-b2 forbids that in its own words. **Every one of the ten needed at least one regeneration; none passed Stage 6 first-pass.** The cause is one check: **`style.tension_collapse` is 16 of 18 rejections (89%)**, and it is not a model failure - it bans `selaras`, `saling melengkapi`, `menyatu`, `berpadu`, which are the ordinary Indonesian for what P3 IS (each chart supplying the other's favourable element). A ban written to stop a MIRROR reading dissolving one person's internal tension into a compliment is firing on a compat reading stating complementarity as a fact. **Recorded as a RULING for Reyner with three options, not fixed** - scoping it off `kind === pair` is the loosening the prompt forbids, and is not obviously right either, since a pair reading genuinely can collapse a tension. **States what it does NOT establish**: ten draws against the mirror's forty, binomial 95% interval roughly 3-56%, so 20% is a direction and a cause rather than a settled rate; a 40-draw replicate is not worth spending until the ruling lands. X-b3's floor-rate precondition is NOT MET. The two new pair checks rejected nothing across all ten. | `X-compat-2b2` | n/a | - |

## Superseded

**Kept, not deleted.** Each was verified cited NOWHERE before being moved -
`grep -rl <name> . --exclude-dir=node_modules --exclude-dir=.git` returned zero. A superseded
artifact that is still cited stays where it is and gets a `Superseded by` row instead, because
moving it would break the citation.

| Artifact | What it answered | Superseded by, and why |
|---|---|---|
| `2026-08-21-floor-readings.md` | Floor rate at n=1 per chart, 08-21. | `2026-08-21-renders-n10.md`. n=1 was found unable to answer the question at all: three consecutive runs of the same four charts returned 0/4, 2/4 and 1/4 with the failing checks identical. |
| `2026-08-21-floor-readings-v2.md` | The same measurement, re-run. | Same. The `-v2` is itself the naming mistake the rule above now blocks: it names the attempt, not the measurement. |
| `2026-08-22-depth-1-vs-3-readings.md` | The depth-pair read, built from the **08-18** retry-depth JSON. | `2026-08-22-depth-1-vs-3-postfixes.md`, which asks the same question on the CURRENT gate and prompt. A depth curve does not carry across a gate change - that is the 08-22 lesson recorded in `lib/render/config.js` - so the older-trace version cannot support the ruling. |
